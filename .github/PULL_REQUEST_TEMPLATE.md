<!-- 以下三个必填段缺一不可;缺段或留空的 PR 将被 code-reviewer 判 FAIL。 -->

## 一、改动摘要(必填)

<!-- 一句话说明做了什么;并列出本次改动涉及/遵循的规则编号(如 CPP-010、REUSE-010、BUILD-011、PY-020)。 -->

- 改动内容:
- 关联规则编号:

## 二、复用检查(必填)

<!-- 按 docs/standards/reuse-policy.md 的搜索记录格式填写,禁止留空;
     纯注释/纯格式改动可写"不适用(无新增实现)"。 -->

```
搜索记录: 关键词=[<词1>, <词2>, ...]; 项目内=<无|命中项及处置>; 第三方=<无|库名及处置> → <直接调用|扩展已有|封装接入|允许新写>
```

## 三、已读文档清单与测试证据(必填)

<!-- 已读文档:列出本次任务按 docs/README.md 路由表读过的 docs/ 文件路径,逐行一个。 -->

- 已读文档:
  - docs/

<!-- 测试证据:粘贴构建测试报告摘要(preset、构建/测试结论、失败摘要、SKIP 清单、警告统计);
     SKIP 清单如实保留,不得删减。 -->

- build-tester 报告摘要:

```
preset:
构建:
测试:
SKIP 清单:
警告统计:
```
